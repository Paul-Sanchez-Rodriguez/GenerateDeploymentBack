---
apiVersion: "v1"
kind: "Service"
metadata:
  labels:
    name: "demo"
  name: "demo"
  namespace: "foh"
spec:
  ports:
  - name: "http"
    port: 8080
    protocol: "TCP"
    targetPort: 8080
  selector:
    name: "demo"
  type: "NodePort"
